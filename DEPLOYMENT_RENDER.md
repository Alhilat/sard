# دليل النشر على منصة Render (Render Deployment Guide)
## منصة سرد رقمي (Sard Raqami Platform)

تم إعداد المشروع بالكامل ليتم نشره على **Render** إما بنقرة واحدة عبر **Blueprints** أو يدوياً كـ **Web Service**.

---

## الطريقة الأولى: النشر بنقرة واحدة عبر Render Blueprint (موصى بها)

1. ارفع الكود إلى مستودعك على **GitHub** أو **GitLab**.
2. سجّل الدخول إلى [Render Dashboard](https://dashboard.render.com).
3. اضغط على زر **New +** ثم اختر **Blueprint**.
4. اختر مستودع المشروع `sard-raqami`.
5. سيقرأ Render ملف `render.yaml` تلقائياً ويقوم بتهيئة الخدمة والمتغيرات البيئية.
6. اضغط **Apply** وسيبدأ البناء والتشغيل فوراً!

---

## الطريقة الثانية: النشر اليدوي كخدمة ويب (Manual Web Service)

إذا رغبت في ضبط الإعدادات يدوياً:
1. في [Render Dashboard](https://dashboard.render.com)، اضغط **New +** ثم اختر **Web Service**.
2. اختر مستودع المشروع.
3. اضبط الإعدادات التالية:
   - **Name**: `sard-raqami`
   - **Environment / Runtime**: `Node`
   - **Region**: الأقرب لك (مثلاً `Frankfurt` أو `Oregon`)
   - **Branch**: `main` (أو الفرع الخاص بك)
   - **Build Command**: `pnpm install && pnpm run build`
   - **Start Command**: `pnpm start`
   - **Plan**: `Free` (أو `Starter`)

4. في قسم **Environment Variables**، أضف التالي:
   | المفتاح (Key) | القيمة الافتراضية (Value) | الوصف |
   |--------------|---------------------------|-------|
   | `NODE_ENV` | `production` | وضع التشغيل الإنتاجي |
   | `JWT_SECRET` | `(اضغط Generate أو اكتب نصاً سرياً عشوائياً)` | توقيع جلسات المستخدمين |
   | `PETRA_USER` | `petra` | اسم مستخدم بوابة الإدارة والتحكم |
   | `PETRA_PASS` | `petra2026` | كلمة مرور بوابة الإدارة والتحكم |

---

## الحسابات التجريبية الجاهزة للاختبار الفوري

بعد تشغيل الموقع، يمكنك الدخول واختبار كافة الميزات بنقرة واحدة عبر أزرار الحسابات التجريبية في صفحة الدخول:

| الحساب | البريد الإلكتروني | كلمة المرور | الدور |
|--------|------------------|-------------|-------|
| **حساب فرد (سارة الأحمد)** | `sara@sard.sa` | `sard123456` | فرد (شبكة اجتماعية، مجموعات، دورات، فعاليات) |
| **حساب فرد (أحمد الزهراني)** | `ahmed@sard.sa` | `sard123456` | فرد |
| **حساب منظمة (رواد التطوع)** | `contact@rwad.org` | `sard123456` | منظمة (لوحة تحكم المنظمات، الأنشطة، الأعضاء) |
| **بوابة بترا المركزية** | الرابط: `/petra` | المستخدم: `petra` / المرور: `petra2026` | بوابة الإشراف وإدارة الحظر والمحتوى |

---

## استمرارية البيانات (Data Persistence)

- محرك البيانات يعمل بتقنية SQLite WAL فائقة السرعة المدمجة مع O(1) Cache.
- في باقات Render المدفوعة، يمكنك إضافة **Persistent Disk** وتعيين مسار التثبيت (Mount Path) إلى `/var/data`؛ سيكتشف المحرك ذلك تلقائياً ويحفظ قاعدة البيانات في القرص الدائم.
- في الباقة المجانية، يتم حفظ البيانات في المجلد المحلي `database/` وتهيئتها تلقائياً عند الإقلاع.
